{
  "name": "subnet-workbench",
  "version": "0.1.0",
  "private": true,
  "description": "Visual-first IPv4 network planning tool: overlap detection, VLSM allocation, multi-vendor config output.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
